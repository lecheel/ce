//! Completion module.
//!
//! Re-exports the primary types at the `comp` level for convenience.

pub mod state;

// pub(crate) use state::Phase;
// pub use state::{find_prefix_overlap, AcceptResult, CompletionMachine};
